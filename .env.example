# Optional. Without this key, the tool uses deterministic rule-based agents.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1

# Optional Vivado executable path. Leave empty for dry-run report generation only.
VIVADO_BIN=
